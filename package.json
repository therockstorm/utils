{
  "name": "@therockstorm/utils",
  "version": "3.0.3",
  "description": "Tiny utility functions",
  "homepage": "https://github.com/therockstorm/utils#readme",
  "bugs": {
    "url": "https://github.com/therockstorm/utils/issues"
  },
  "license": "MIT",
  "author": {
    "name": "Rocky Warren",
    "url": "https://www.rockywarren.com"
  },
  "files": [
    "lib"
  ],
  "main": "lib/src/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/therockstorm/utils.git"
  },
  "scripts": {
    "build": "rm -rf lib && tsc --outDir lib && cp src/index.d.ts lib/src",
    "format": "prettier --write '{,!(lib)/**/}*.+(js|json|md|ts)'",
    "prepare": "npm run build",
    "pretest": "npm run format",
    "start": "tsc && node src/index.js",
    "test": "mocha -r ts-node/register test/**/*.ts",
    "watch": "npm test -- --watch"
  },
  "devDependencies": {
    "@types/mocha": "^5.2.5",
    "@types/node": "^11.9.3",
    "husky": "^1.3.1",
    "lint-staged": "^8.1.3",
    "mocha": "^5.2.0",
    "prettier": "^1.16.4",
    "ts-node": "^8.0.2",
    "typescript": "^3.3.3"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,json,md,ts}": [
      "prettier --write",
      "git add"
    ]
  },
  "prettier": {
    "semi": false,
    "singleQuote": true
  }
}
