{
  "name": "@therockstorm/utils",
  "version": "2.0.10",
  "description": "Common utility functions",
  "homepage": "https://github.com/therockstorm/utils#readme",
  "bugs": {
    "url": "https://github.com/therockstorm/utils/issues"
  },
  "license": "MIT",
  "author": {
    "name": "Rocky Warren",
    "url": "https://www.rockywarren.com"
  },
  "files": [
    "lib"
  ],
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/therockstorm/utils.git"
  },
  "scripts": {
    "build": "rm -rf lib && tsc --outDir lib && cp src/index.d.ts lib",
    "precommit": "lint-staged",
    "format": "prettier --write '{,!(lib)/**/}*.+(js|json|md|ts)'",
    "prepare": "npm run build",
    "pretest": "npm run format",
    "test": "mocha -r ts-node/register",
    "watch": "npm test -- --watch"
  },
  "devDependencies": {
    "@types/mocha": "^5.2.5",
    "@types/node": "^10.9.2",
    "husky": "^0.14.3",
    "lint-staged": "^7.2.0",
    "mocha": "^5.2.0",
    "prettier": "^1.13.7",
    "sinon": "^6.1.4",
    "ts-node": "^7.0.1",
    "typescript": "^3.0.1"
  },
  "lint-staged": {
    "*.{js,json,md,ts}": [
      "prettier --write",
      "git add"
    ]
  },
  "prettier": {
    "semi": false,
    "singleQuote": true
  }
}
